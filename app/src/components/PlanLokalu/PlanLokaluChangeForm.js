import React, {Component} from 'react';
import { StyleSheet, Text, View, TouchableOpacity, SectionList } from 'react-native'
import {Icon, Button, CheckBox, Divider, Overlay} from 'react-native-elements'

class PlanLokaluChangeForm extends Component {
    state = {
        isVisible: true,
    }
    render (){
        return (
            <Text> Siema tu coś tam </Text>
        )
    }
};
export default PlanLokaluChangeForm;